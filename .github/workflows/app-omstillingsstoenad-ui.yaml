name: omstillingsstoenad-ui

on:
  workflow_dispatch: # Allow manually triggered workflow run
    inputs:
      deploy-prod:
        description: "Deploy til produksjon"
        required: false
        default: 'false'
        type: choice
        options:
          - true
          - false
  push:
    branches:
      - main
    paths:
      - apps/omstillingsstoenad-ui/**
  pull_request:
    branches:
      - main
    paths:
      - apps/omstillingsstoenad-ui/**

jobs:
  build:
    uses: ./.github/workflows/.build-frontend.yaml
    secrets: inherit

  test:
    needs: build
    uses: ./.github/workflows/.test-frontend.yaml
    with:
      yarn-lock-hash: ${{ needs.build.outputs.yarn-lock-hash }}
    secrets: inherit

  # Må byttes tilbake til å bruke deploy.yaml når vi går i prod
  deploy-to-dev-gcp:
    name: dev-gcp
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-gcp
          RESOURCE: apps/${{ github.workflow }}/.nais/dev.yaml
          VAR: image=${{ inputs.image }}
