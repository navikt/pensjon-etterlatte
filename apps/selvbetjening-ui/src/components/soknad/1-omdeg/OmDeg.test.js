import { render } from "@testing-library/react";

describe("OmDeg", () => {
    it("skal teste om deg", () => {
        /* const neste = jest.fn();
        Modal.setAppElement = () => null;

        const { container } = render(<OmDeg neste={neste} />);
        //expect(container).toMatchSnapshot();*/
    });
});
